#include <stdio.h>

int main(){
    float score = 90.5 ;
    printf("float score = %.1f", score );
    return 0 ;
}
//flaot แสดงผล = 90.5 มีเลขทศนิยม

//1.ผลลัพธ์ต่างกันคือ int จะเก็บค่าเป็นเลขจำนวนเต็มไม่มีจุดทศนิยม , float  จะเก็บค่าเป็นจุดทศนิยม
//2.เลือก DATA TYPE ให้ถูกต้องเพราะ จะได้เก็บและแสดงค่าได้อย่างถูกต้องตามที่ต้องการ
//3.สรุป ถ้าต้องการใช้ค่าที่เป็นจำนวนเต็มให้ใช้ int แต่ถ้าต้องการค่าที่เป็นจุดทศนิยมให้ใช้ float 